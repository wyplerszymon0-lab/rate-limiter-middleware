{
  "name": "rate-limiter-middleware",
  "version": "1.0.0",
  "description": "Sliding window rate limiter middleware for Express. Pluggable store (memory or Redis).",
  "main": "index.js",
  "scripts": {
    "test": "node tests/rate-limiter.test.js",
    "example": "node examples/server.js"
  },
  "keywords": ["rate-limit", "express", "middleware", "sliding-window", "redis"],
  "license": "MIT",
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "optionalDependencies": {
    "ioredis": ">=5.0.0"
  },
  "devDependencies": {
    "express": "^4.18.2"
  }
}
